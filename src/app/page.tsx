import
	SkillsCategoryGrid
from "./_components/skill"
import {
	fetchSkills
} from "./_components/fetch"

export default async function HomePage({

}:{

}) {
	return null;
	// const skills = await fetchSkills();

	// return (
	// 	<>
	// 	{skills.map((skill) => (
	// 		<SkillsCategoryGrid
	// 			key={skill.name}
	// 			skill_name={skill.name}
	// 			modules_used={skill.modules_used}
	// 			at_projects={skill.at_projects}
	// 		/>
	// 	))}
	// 	</>
	// );
}